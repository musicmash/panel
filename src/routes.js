import Vue from "vue";
import VueRouter from "vue-router";
import "firebase/auth";

Vue.use(VueRouter);

const routes = [

];

const router = new VueRouter({
    routes,
    mode: "history",
});

export default router;
